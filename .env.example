# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Prisma Database
# For local SQLite (development):
DATABASE_URL="file:./db.sqlite"

# For Turso (production):
# DATABASE_URL="libsql://[your-database]-[your-org].turso.io"
# DATABASE_AUTH_TOKEN="your-turso-auth-token"

# To set up Turso:
# 1. Install Turso CLI: Download from https://github.com/tursodatabase/turso-cli/releases
# 2. Login: turso auth login
# 3. Create database: turso db create hfh-team-builder
# 4. Get URL: turso db show hfh-team-builder --url
# 5. Get token: turso db tokens create hfh-team-builder
# 6. Update DATABASE_URL and DATABASE_AUTH_TOKEN above
# 7. Run: bun run db:push

# AI Integration (Claude)
# ANTHROPIC_API_KEY="your-claude-api-key-here"

# Application
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
